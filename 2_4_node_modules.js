// core modules
const http = require('http');

// local module
const { name } = require('./2_4z_local_module');

// third-party module
const _ = require('underscore');

const server = http.createServer(function (req, res) {
    console.log(`Server started.`);
});
server.listen(5000);

console.log(name);

var stooges = [
    { name: 'moe', age: 40 },
    { name: 'larry', age: 50 },
    { name: 'curly', age: 60 },
];
const result = _.pluck(stooges, 'name');
console.log(result);
